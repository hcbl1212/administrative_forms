<%= sar_form.label(nil, label_name) %>
<%= sar_form.collection_check_boxes(:group_ids, group_collection, :id, :name) do | item_group | %>
    <%= item_group.label { item_group.check_box + item_group.text } %>
<% end %>
